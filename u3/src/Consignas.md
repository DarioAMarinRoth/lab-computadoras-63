# Tareas

## 19/09/24

1. Realizar una función `esta()` que reciba como parámetro un vector de enteros y un valor entero. La función debe devolver true si el valor se encuentra en el vector y false si no se encuentra.
2. Realizar una función `subVector()` que reciba como parámetros un vector de enteros y dos enteros `left` y `right` y que devuelva el subVector que comienza en el índicie `left` y termina en el índice `right` del vector original.